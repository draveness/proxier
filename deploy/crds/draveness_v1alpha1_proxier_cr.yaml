apiVersion: draveness.me/v1alpha1
kind: Proxier
metadata:
  name: example-proxier
spec:
  servers:
    - name: v1
      proportion: 0.1
      targetPort: 5678
      selector:
        version: v1
        app: test
    - name: v2
      proportion: 0.9
      targetPort: 5678
      selector:
        version: v2
        app: test
